
syntax = "proto3";

package com.events;

service EventService {
  rpc CreateEvent(CreateEventRequest) returns (CreateEventResponse) {}
  rpc FindAllEvents(FindAllEventsRequest) returns (FindAllEventsResponse) {}
  rpc FindOneEvent(FindOneEventRequest) returns (FindOneEventResponse) {}
}

message CreateEventRequest {
  string tenantId = 1;
  string eventName = 2;
  string exchangeName = 3;
}

message Event {
  string event_id = 1;
  string tenantId = 2;
  string eventName = 3;
  string exchangeName = 4;
}

message CreateEventResponse {
  Event event = 1;
}

message FindAllEventsRequest {}

message FindAllEventsResponse {
  repeated Event event = 1;
}

message FindOneEventRequest {
  string event_id = 1;
}

message FindOneEventResponse {
  // Corrija para o tipo apropriado, presumindo que seja uma mensagem 'Order'.
  // Se for outra mensagem, substitua 'Order' pelo nome apropriado.
  Event order = 1;
}
